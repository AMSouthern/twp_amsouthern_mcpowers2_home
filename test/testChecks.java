import edu.bsu.cs222.XML.UseXML;
import edu.bsu.cs222.XML.Wikipedia;
import org.junit.Assert;
import org.junit.Test;

/**
 * @ authors: Alexandria Southern and Marley Powers
 *
 * CS 222 - S2 David Largent
 * February 14, 2017
 *
 * This class tests all of the methods implemented in Checks.
 * However, I'm having trouble importing that class.
 */
public class testChecks {
/*
    @Test
    public void testPositivePageExist() throws Exception {
        UseXML parser = new UseXML ();
        Wikipedia wikiPage = new Wikipedia();

        wikiPage.createHistoryOfQueryURL("Soup");
        wikiPage.checkForInternetConnection();
        parser.parse(wikiPage.returnInputStream());
        Assert.assertTrue(parser.checkIfPageExists());
    }


    @Test
    public void testNegativePageExist() throws Exception {
        UseXML parser = new UseXML();
        Wikipedia wikiPage = new Wikipedia();

        wikiPage.createHistoryOfQueryURL("Bowling Pinheads");
        wikiPage.checkForInternetConnection();
        parser.parse(wikiPage.returnInputStream());
        Assert.assertFalse(parser.checkIfPageExists());
    }
*/
}
